-- Dev seed data (run manually, never in production)
-- Requires a real auth.users entry — use Supabase dashboard to create the user first,
-- then replace the UUID below with the actual user id.

-- INSERT INTO public.projects (user_id, name, description, status)
-- VALUES
--     ('replace-with-real-user-uuid', 'Sample Anarkali', 'Test project for dev', 'draft'),
--     ('replace-with-real-user-uuid', 'Summer Kurta', 'Casual cotton kurta', 'spec_ready');
